# Instance identification (required)
CLIENT_ID=home-nas

# Token for authentication (must match KV instance:<client_id>)
TOKEN=your_secret_token_here

# Workers URL for API calls
WORKER_URL=https://leex-ip-optimizer.xledoo.workers.dev
